var Wait = new Phaser.Class({
    Extends: Phaser.Scene,

    initialize: function Wait() {
        Phaser.Scene.call(this, { key: "Wait" });
    },

    preload() {},

    create() {
        /*this.tenta2.on("pointerup", () => {
            this.scene.start("WorldScene"), 300;
        });*/
    },
});
